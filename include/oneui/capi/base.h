#pragma once

// Stable base entry point for OneUI's C ABI. The umbrella remains the source
// of declarations so existing consumers retain identical types and symbols.
#include "oneui/oneui_c_api.h"
